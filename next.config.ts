import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Phase 1: static-friendly storefront shell over mock data.
  // No remote images: product visuals are local CSS placeholders.
};

export default nextConfig;
